/*Copyright (c) 2015-2016 wavemaker-com All Rights Reserved.
 This software is the confidential and proprietary information of wavemaker-com You shall not disclose such Confidential Information and shall use it only in accordance
 with the terms of the source code license agreement you entered into with wavemaker-com*/
package com.civicxpress.cx2.service;

/*This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/


import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wavemaker.runtime.data.exception.EntityNotFoundException;
import com.wavemaker.runtime.data.export.ExportType;
import com.wavemaker.runtime.data.expression.QueryFilter;
import com.wavemaker.runtime.file.model.Downloadable;

import com.civicxpress.cx2.ContractorApprovals;

/**
 * Service object for domain model class {@link ContractorApprovals}.
 */
public interface ContractorApprovalsService {

    /**
     * Creates a new ContractorApprovals. It does cascade insert for all the children in a single transaction.
     *
     * This method overrides the input field values using Server side or database managed properties defined on ContractorApprovals if any.
     *
     * @param contractorApprovals Details of the ContractorApprovals to be created; value cannot be null.
     * @return The newly created ContractorApprovals.
     */
	ContractorApprovals create(ContractorApprovals contractorApprovals);


	/**
	 * Returns ContractorApprovals by given id if exists.
	 *
	 * @param contractorapprovalsId The id of the ContractorApprovals to get; value cannot be null.
	 * @return ContractorApprovals associated with the given contractorapprovalsId.
     * @throws EntityNotFoundException If no ContractorApprovals is found.
	 */
	ContractorApprovals getById(Integer contractorapprovalsId) throws EntityNotFoundException;

    /**
	 * Find and return the ContractorApprovals by given id if exists, returns null otherwise.
	 *
	 * @param contractorapprovalsId The id of the ContractorApprovals to get; value cannot be null.
	 * @return ContractorApprovals associated with the given contractorapprovalsId.
	 */
	ContractorApprovals findById(Integer contractorapprovalsId);


	/**
	 * Updates the details of an existing ContractorApprovals. It replaces all fields of the existing ContractorApprovals with the given contractorApprovals.
	 *
     * This method overrides the input field values using Server side or database managed properties defined on ContractorApprovals if any.
     *
	 * @param contractorApprovals The details of the ContractorApprovals to be updated; value cannot be null.
	 * @return The updated ContractorApprovals.
	 * @throws EntityNotFoundException if no ContractorApprovals is found with given input.
	 */
	ContractorApprovals update(ContractorApprovals contractorApprovals) throws EntityNotFoundException;

    /**
	 * Deletes an existing ContractorApprovals with the given id.
	 *
	 * @param contractorapprovalsId The id of the ContractorApprovals to be deleted; value cannot be null.
	 * @return The deleted ContractorApprovals.
	 * @throws EntityNotFoundException if no ContractorApprovals found with the given id.
	 */
	ContractorApprovals delete(Integer contractorapprovalsId) throws EntityNotFoundException;

	/**
	 * Find all ContractorApprovals matching the given QueryFilter(s).
     * All the QueryFilter(s) are ANDed to filter the results.
     * This method returns Paginated results.
	 *
     * @deprecated Use {@link #findAll(String, Pageable)} instead.
	 *
     * @param queryFilters Array of queryFilters to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching ContractorApprovals.
     *
     * @see QueryFilter
     * @see Pageable
     * @see Page
	 */
    @Deprecated
	Page<ContractorApprovals> findAll(QueryFilter[] queryFilters, Pageable pageable);

    /**
	 * Find all ContractorApprovals matching the given input query. This method returns Paginated results.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching ContractorApprovals.
     *
     * @see Pageable
     * @see Page
	 */
    Page<ContractorApprovals> findAll(String query, Pageable pageable);

    /**
	 * Exports all ContractorApprovals matching the given input query to the given exportType format.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param exportType The format in which to export the data; value cannot be null.
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return The Downloadable file in given export type.
     *
     * @see Pageable
     * @see ExportType
     * @see Downloadable
	 */
    Downloadable export(ExportType exportType, String query, Pageable pageable);

	/**
	 * Retrieve the count of the ContractorApprovals in the repository with matching query.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query query to filter results. No filters applied if the input is null/empty.
	 * @return The count of the ContractorApprovals.
	 */
	long count(String query);


}

